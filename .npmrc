auto-install-peers=true
strict-peer-dependencies=false

# Block packages published less than 7 days ago (supply chain protection)
min-release-age=7

# Block all install scripts by default (supply chain protection)
# Packages that legitimately need build scripts must be allowlisted
# via pnpm.onlyBuiltDependencies in the root package.json.
ignore-scripts=true
