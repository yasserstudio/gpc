{
  "name": "e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests --testTimeout=20000",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@gpc-cli/cli": "workspace:*"
  }
}
